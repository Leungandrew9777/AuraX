# Project Augo - Python Dependencies (Windows-compatible)

# Core data processing
polars>=0.20.0
numpy>=1.24.0
pandas>=2.0.0

# Machine Learning
xgboost>=2.0.0
scikit-learn>=1.3.0

# Database
psycopg2-binary>=2.9.9  # PostgreSQL adapter (use psycopg2 for production)

# HTTP & RSS
requests>=2.31.0
feedparser>=6.0.10

# Validation & Settings
pydantic>=2.5.0
pydantic-settings>=2.1.0

# LLM Local (Ollama client - uses requests)
# No additional package needed, using direct HTTP API

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# GUI (included with Python on Windows via python.org installer)
# tkinter comes bundled with Python on Windows

# Optional: For enhanced logging
python-json-logger>=2.0.7

# Optional: For scheduling (if running predictions automatically)
schedule>=1.2.0
